{
  "name": "drive",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/drive",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "apps/drive/.next",
        "cwd": "apps/drive",
        "command": "pnpm pre next build"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/drive"
        },
        "production": {}
      }
    },

    "serve": {
      "executor": "nx:run-commands",
      "defaultConfiguration": "dev",
      "options": {
        "cwd": "apps/drive"
      },
      "configurations": {
        "dev": {
          "command": "pnpm pre next dev"
        },
        "prod": {
          "command": "pnpm pre next start"
        }
      }
    },
    "export": {
      "executor": "@nx/next:export",
      "options": {
        "buildTarget": "drive:build:production"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/drive/jest.config.ts",
        "passWithNoTests": true
      },
      "configurations": {
        "ci": {
          "ci": true,
          "codeCoverage": true
        }
      }
    },
    "lint": {
      "executor": "@nx/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/drive/**/*.{ts,tsx,js,jsx}"]
      }
    },
    "serve:bg": {
      "executor": "nx:run-commands",
      "options": {
        "command": "prisma studio",
        "cwd": "apps/drive"
      }
    }
  },
  "tags": ["drive"]
}
